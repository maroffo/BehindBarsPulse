# ABOUTME: Example environment variables for local development.
# ABOUTME: Copy to .env and fill in your values.

# Database (used by docker-compose and app)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=behindbars
DB_USER=behindbars
DB_PASSWORD=behindbars_local

# Gemini API (required for AI features)
GEMINI_API_KEY=your_gemini_api_key

# Email (AWS SES - required for sending newsletters)
# ses_usr=your_ses_username
# ses_pwd=your_ses_password

# Optional overrides
# GEMINI_MODEL=gemini-2.0-flash-exp
# AI_SLEEP_BETWEEN_CALLS=30
# MAX_ARTICLES=100
